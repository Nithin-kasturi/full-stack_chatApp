apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chat-app-helm.fullname" . }}-test-connection"
  labels:
    {{- include "chat-app-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chat-app-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
